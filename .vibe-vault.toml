# vibe-vault project identity
# Uncomment and set values to override automatic detection.
# When commented out, vv uses heuristics (git remote name, directory basename).

[project]
name = "rezbldr"
domain = "personal"          # e.g. "work", "personal", "opensource", or custom
tags = ["go", "cli", "vibe-coding", "obsidian", "resume"]            # e.g. ["go", "cli", "obsidian"]

[meta]
author = "John Suykerbuyk"
company = "SykeTech LTD"
